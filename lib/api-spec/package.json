{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node -e \"const fs=require('fs'),f='../../lib/api-zod/src/index.ts';fs.writeFileSync(f,fs.readFileSync(f,'utf8').replace(/.*api\\.schemas.*/g,'').trim()+'\\n')\" && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.5.2"
  }
}
